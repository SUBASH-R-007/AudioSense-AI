Drop the Hindi speech sample here as `speech_sample.wav`.

Record one clear Hindi sentence at the same RMS level as
`public/audio/speech_sample.wav` (the English original), 44.1 kHz mono WAV.
The simulator prefers this file whenever the interface language is Hindi and
falls back to the English sample until it exists. See SAMPLE_URLS in
`src/pages/Simulator.jsx`.
